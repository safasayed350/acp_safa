I want to build a 2D graphics editor that runs in the Windows terminal. It should use only standard Windows headers like <windows.h> and <conio.h>, nothing extra to install. Please force the console to 80x25 so it fits inside VS Code terminal without scrolling.
For the layout I want a title bar at the top, then a canvas on the left side that is 57 columns wide and 18 rows tall with a border around it, and a menu panel on the right side. Below everything there should be a status line and two rows for input. The canvas should be stored as a plain 2D char array and shapes should be drawn using only * and _ characters.
I need four shapes: a circle drawn with the midpoint algorithm, a rectangle as just the outline, a line using Bresenham, and a triangle made from three Bresenham lines. Keep all shapes in an array of structs with an active flag so I can soft delete without shifting anything.
The menu should use keys 1 through 8 plus q for: add circle, add rectangle, add line, add triangle, delete, modify, list, clear and quit. When modifying a shape it should re-ask all the coordinate fields depending on what type it is. The object list and count should appear inside the menu panel itself, not below the canvas.
For input use _getch() for the menu and scanf() for coordinates. Use SetConsoleCursorPosition to move the cursor around instead of printing newlines. Clear and fully redraw the screen after every action. Hide the cursor while drawing and show it only when waiting for input. Make the border green, menu yellow, status line red, title bar blue, and color * pixels cyan and _ pixels yellow.
